// 1. for a given array, make each odd number even and each even number odd


scores = [10, 15, 9, 7, 12]

function maxAndMinGames(scores) {
  let max = 0
  let min = 0
  let currMax = scores[0]
  let currMin = scores[0]
  let finalArray = [] // [min, max] scores

  for(let i = 1; i < scores.length; i++) {

  }
}
